server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — every unmatched route serves index.html so client-side
    # state (which page you're on) survives a refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
